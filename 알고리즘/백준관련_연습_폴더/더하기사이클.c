#include<stdio.h>

int main(){
    int a, b;
    scanf("%1d%1d", &a, &b);
    // printf("%d %d", a, b);
    int sum = a+b;
    while (sum = b)
    {
        
    }
    
     

    printf("%d", sum);
    return 0;
}