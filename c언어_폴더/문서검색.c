// 브루트포스 알고리즘(완전탐색) 사용
#include<stdio.h>

int main(){
    char document[2500];
    char search[50];
    scanf("%c", &document);
    scanf("%c", &search);
    int a, b;
    a = search.length;
    b = ;
    while (document!=EOF){
        document
        if (search[a] == document[a])
        {
            /* code */
        }
        
    }

    return 0;
}